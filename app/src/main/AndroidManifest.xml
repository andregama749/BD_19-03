<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atividade.bd_19_03">

    <application
        android:name=".View.Configuracao"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.PecasActivity"
            android:label="@string/title_activity_pecas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.PecaDetalheActivity" />
        <activity
            android:name=".View.ProActivity"
            android:label="@string/title_activity_pro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.ProDetalheActivity" />
        <activity
            android:name=".View.SerActivity"
            android:label="@string/title_activity_ser"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.SerDetalheActivity"></activity>
    </application>

</manifest>